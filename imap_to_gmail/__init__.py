"""
IMAP to Gmail API Import Module

This module reads emails from an IMAP server and imports them into Gmail
using the Gmail API's import functionality, which preserves email metadata
better than IMAP append.
"""

__version__ = "1.0.0"
